<Window x:Class="FUNewsClientWpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="260" Width="380" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Sign in" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <TextBox x:Name="EmailBox" Grid.Row="1" Style="{StaticResource WatermarkTextBox}" Tag="Email address" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Password" Margin="0,4,0,4" Foreground="{StaticResource GrayTextBrush}"/>
            <PasswordBox x:Name="PasswordBox"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="0,16,0,0" HorizontalAlignment="Right">
            <Button Content="Login" Width="110" Click="Login_Click" Style="{StaticResource PrimaryButton}" />
        </StackPanel>
    </Grid>
</Window>