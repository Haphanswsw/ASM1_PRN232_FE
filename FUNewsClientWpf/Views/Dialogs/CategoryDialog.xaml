<Window x:Class="FUNewsClientWpf.Views.Dialogs.CategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Category" Height="260" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="NameBox" Grid.Row="0" Style="{StaticResource WatermarkTextBox}" Tag="Name" Margin="0,0,0,8"/>
        <TextBox x:Name="DescBox" Grid.Row="1" Style="{StaticResource WatermarkTextBox}" Tag="Description" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4,0,0">
            <TextBlock Text="Parent ID:" VerticalAlignment="Center"/>
            <TextBox x:Name="ParentBox" Width="120" Margin="6,0,0,0"/>
            <CheckBox x:Name="ActiveBox" Content="Active" Margin="12,0,0,0" IsChecked="True"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Width="90" Margin="0,0,8,0" Click="Ok_Click" Style="{StaticResource PrimaryButton}"/>
            <Button Content="Cancel" Width="90" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>